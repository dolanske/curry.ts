{
  "name": "curryts",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "c8": "^7.11.3",
    "typescript": "^4.5.4",
    "vite": "^2.8.0",
    "vitest": "^0.17.1"
  },
  "dependencies": {
    "csstype": "^3.1.0"
  }
}
