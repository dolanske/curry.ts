import { $ } from './curry'

// $('.rect').first().slideUp()

$('button').click(async () => {
  // await $(this).next().children().first().slideUp(5).await

  // $(this).next().children().slideToggle({
  //   override: true,
  // })
})
